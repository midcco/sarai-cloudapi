# Sarai Cloud API

Una API REST construida con Node.js para manejar (descripción corta de lo que hace).

## Tecnologías usadas
- Node.js
- JavaScript
- Express (si lo usas)
- (otras librerías que uses)

## Cómo instalar
1. Clona el repositorio
2. Ejecuta `npm install`
3. Corre con `npm start` o `node index.js`

## Endpoints
- `GET /` – Descripción corta
- `POST /webhook` – lo que hace, cómo usar, etc.

## Contribuciones
Con porcentaje

